function Lobby({ setScene }) {
    return (
        <div className="lobby">
            <h1>Lobby</h1>
            <button id="start-game">Start Game</button>
        </div>
    )
}

export default Lobby